// Kept in sync by hand with app.json's "version"/"android.versionCode" on
// each release — no expo-constants dependency for just this. Shown in
// Settings so "which build is actually installed on this phone" is a
// 2-second check instead of a guess next time something looks broken
// right after an update.
export const APP_VERSION = '1.9.2';
export const APP_BUILD = 14;
export const ANYWAY_PROTOCOL_VERSION = 1;
